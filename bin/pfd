#!/usr/bin/env bash

# Print a file/dir's absolute path and copy it to the clipboard.
# Usage: pfd [path]   (defaults to .)

path=$(realpath "${1:-.}")
echo "$path"

b64=$(printf '%s' "$path" | base64 | tr -d '\n')
printf '\033]52;c;%s\a' "$b64" > /dev/tty
